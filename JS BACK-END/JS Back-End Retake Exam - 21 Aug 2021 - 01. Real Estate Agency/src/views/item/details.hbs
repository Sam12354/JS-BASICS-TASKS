<section id="deatils-page">
    <div class="wrapper">
        <div class="product-img">
            <img src="{{item.image}}">
        </div>
        <div class="product-info">
            <div class="product-text">
                <div class="product-text">
                    <h1>Name: {{item.name}}</h1>
                    <h2>Type: {{item.type}}</h2>
                    <h4>Year: {{item.year}}</h4>
                    <h4>City: {{item.city}}</h4>
                    <p>Description: {{item.description}}</p>
                
                    <!-- if there are already tenants of the housing, separate their names with a comma and a space ", "  -->
                    {{#if noTentants}}
                    <p>People rented this housing: There are no tenants yet.</p>
                    {{else}}
                    <p>People rented this housing: {{allPeopleRented}}</p>
                    <!-- If not display: -->
                    {{/if}}
                
                </div>
            </div>

            <!-- if there is no registered user, do not display buttons-->
            {{#if isAuthenticated}}
            <div class="product-btn">
            {{#if isOwner}}
                <!-- Only for registered user and creator of the housing offer-->
                <a href="/item/{{item._id}}/edit" class="edit">Edit</a>
                <a href="/item/{{item._id}}/delete" class="remove">Delete</a>
            {{else}}
            {{#if isVoted}}
                <!-- logged in user with no available pieces-->
                {{#if outOfSpace}}
                <p class="no-housing">No Housing Available!</p>
                {{else}}
                <p class="alRentHome">You have already rent this home</p>
                {{/if}}
            {{else}}
                {{#if outOfSpace}}
                <p class="no-housing">No Housing Available!</p>
                {{else}}
                <!-- logged in user with available pieces-->
                <a href="/item/{{item._id}}/like" class="rentHome">Rent a home, available {{item.pieces}} housing</a>
                {{/if}}
            </div>
            {{/if}}
            {{/if}}
            {{/if}}
        </div>
    </div>
</section>